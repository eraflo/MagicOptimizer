{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the main window. Everything the app does goes through its own commands, so no plugin capability is granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
